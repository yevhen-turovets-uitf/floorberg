import styled from 'styled-components';
import * as GS from 'src/styles/styles';
import * as LS from '../../styles';

export const CounterpartiesTitle = styled(GS.FlexContainer)`
  background: #FBFBFB;
  border-bottom: 1px solid #E1E1E1;
  padding: 10px 15px 10px 16px;
`;

export const FlexConteiner = styled(GS.FlexContainer)`
  text-align: right;
`;

export const Role = styled.div`
  overflow-wrap: normal;
  word-break: normal;
`;

export const LegalPersonLink = styled.a`
  color: #2F80ED;
  margin: 0 5px 0 0;
  text-decoration: none;
`;

export const MenuItem = styled(LS.MenuItem)``;
